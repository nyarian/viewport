name: example
description: Example project demonstrating the use of viewport package
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.1.0-nullsafety.0

environment:
  sdk: ">=2.12.0-133.2.beta <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  viewport:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
